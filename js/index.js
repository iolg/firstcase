// 'use strict';

window.onload = function (ev) {
    init(function () {
        let iBner = document.getElementById('m-ibner');
        // 执行banner轮播
        chgBner(iBner, 'm-ibner-', 4);
        console.log('chgBner...');
    });


};

